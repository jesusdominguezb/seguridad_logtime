#! /bin/bash
  
if [ "$1" == "-h" ]
then
    echo "Soy ayuda"

elif [ "$#" > "1" ]
then
    if [ "$1" == "-f" ]
    then
        cat $2
    elif [ "$1" == "-u" ]
    then
        grep $2 $4
    fi
fi
